package com.anubhav;

public class Primitives {
    public static void main(String[] args) {

        // primitives are data types that we cannot break any further

        int integer = 64; // primitive
        String name = "Anubhav"; // not primitive as we can break it into characters (char)

        char letter = 'r';
        float decimalNumbers = 98.67f;
        double largeDecimalNumbers = 456789.4567;
        long largeInteger = 38477464865836L;
        boolean check = true;
        boolean notCheck = false;

        // all the decimal values are by default in double, if we want to store them in float, we add f
        // all the integer valies are by default in int, if we want to store them in long, we add L

        // float can sometime give errors (floating point errors), so for more accurate value, use double

        // TIP: ctrl+'/' to comment everything selected

    }

}
